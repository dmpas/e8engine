Перем Переменная;

Процедура Установить Или УстановитьПеременную Or SetThis(Значение)
	Переменная  = Значение;
КонецПроцедуры

Установить(1);
УстановитьПеременную(2);
SetThis(3);

Заявить Переменная = 3 КАК "Работа синонимов";
