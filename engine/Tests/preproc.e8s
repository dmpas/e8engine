Проверка = Ложь;
#If Client Then
	ВызватьИсключение "Клиент!";
#ElseIf Script Then
	Проверка = Истина;
#EndIf

Заявить Проверка;

Проверка = Ложь;
#If Client Or Script Then
    
    #If Client Then
        ВызватьИсключение "Клиент!";
    #Else
        
        #If Not Server Then
            Проверка = Истина;
        #EndIf
    #EndIf
#ElseIf Script Then
    ВызватьИсключение "(ИЛИ)";
#Else
    ВызватьИсключение "НеСкрипт";
#EndIf

НекаяПеременная = 
	#Если Скрипт Тогда
		"это скрипт"
	#Иначе
		"это не скрипт"
	#КонецЕсли
;

Заявить НекаяПеременная = "это скрипт";
